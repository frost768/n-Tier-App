using System.ComponentModel.DataAnnotations;

public enum Enum_MusteriTipi
{
    Aktif,
    Potansiyel,
    Pasif
}

public enum Enum_Gorev
{
   Muhasebe,
   Finans,
   GenelMudur
}

public enum Enum_RemoteType
{
    TeamViewer,
    AnyDesk,
    Alpemix
}

public enum Enum_ParaBirimi
{
    TL,
    USD,
    EUR
}

public enum Enum_KullaniciTuru
{
    
    SuperAdmin,
    Admin,
    Operator
}